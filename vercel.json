{
  "buildCommand": "npm install --include=dev && npm run build",
  "outputDirectory": "dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://onedegree-estimator.onrender.com/api/$1" }
  ]
}
